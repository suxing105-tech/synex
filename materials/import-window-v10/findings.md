# 导入弹窗与 ComfyUI 标题栏

- 导入窗口的两个次要按钮原本使用 hover:border-accent，改为 enabled:hover:bg-white/10，保持边框颜色不变。
- 导入结束原本只显示完成消息。现在成功且图库刷新结束后关闭绑定的 open 状态；扫描或刷新失败继续显示错误，空目录成功也关闭。
- ComfyUI 子窗口原本没有继承主窗口的主题与原生标题栏设置。现在先以隐藏状态创建 Dark 窗口，调用现有 window_style::apply，再显示，沿用主窗口 #202020 标题栏和浅色文字。
- 回归覆盖空目录成功关闭、正常导入等待刷新完成才关闭、扫描失败保留、刷新失败保留，以及原有目录选择流程。
- 前端 376 项测试通过；svelte-check 0 errors，15 项既有 warnings。
