# 设置交互调整记录

用户要求：设置相关控件的悬停、选中统一采用底色变化，不使用高亮边框。

检查结果：设置分类存在动态品牌边框；通用与 ComfyUI 按钮、输入框存在 hover/focus 边框；模型选择、反推语言和历史记录存在红色选中边框。更新面板缺少一致的悬停反馈。

实现：共享 fill-interactions 样式，保持中性边框，普通悬停使用 surface-3，选中使用品牌红底配深色文字，键盘焦点用底色加下划线。设置各子面板与反推面板共用；顶部设置/导入入口改用底色反馈。禁用控件不参与悬停，支持减少动画偏好。
