# 全屏设置与纯图标入口

按用户要求，将侧栏设置入口改为无可见文字的线条 SVG 齿轮。按钮 48×48，图标 28×28，在 96px 高的底部区域垂直居中、靠左放置，底边间距 24px，保留 aria-label 和 tooltip。

设置采用占满应用窗口的页面：左侧 240px 分类区，右侧独立滚动、最大 960px 的正文区，标题与留白放大。窄屏缩减导航和正文间距，选中和悬停继续使用底色。

不依赖 Codex 内部实现，仅参考用户提出的全屏设置交互方向。
