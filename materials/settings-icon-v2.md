# 设置图标简化记录

参考用户提供的截图：外部为圆角六边形，内部为圆环。采用 SVG currentColor 描边，图案由 28px 缩至 14px，保持原 48px 点击区域和对齐。

本次用户明确要求入口悬停改变图案颜色，因此入口改用 hover:text-accent，取消底色变化；键盘聚焦同样显示品牌色。其他设置选项保持原交互。

移除 FolderTree 底部统计条及其分隔线，保留文件夹本身的计数和图库主内容统计。
