# 设置布局与快捷键调研

现有设置弹窗使用顶部分类，图库键盘逻辑分散于 DetailPanel、Feed、Lightbox。采用左侧固定分类、右侧独立滚动布局，并保留底色交互。

自定义键以动作 ID 关联，运行时读取同一 Svelte store，保存到当前应用的 localStorage；不改后端配置协议。Esc 和 Tab 保留原生关闭/焦点职责。冲突校验覆盖所有可自定义动作；空值表示禁用。

预览的 Space 同时承担打开预览和预览下一张，保留原操作习惯；方向键可分别修改。输入框、设置弹窗、输入法组合及长按屏蔽图库快捷键。
