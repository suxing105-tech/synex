# 桌面标题栏与 Logo

- 原生标题栏固定 Dark 主题，实际窗口已确认显示深灰色，窗口控制按钮保持可见。
- 标题栏、应用图标、NSIS 安装图标、页面品牌和启动页统一使用指定 Logo。
- 前端测试：32 个测试文件、285 项测试全部通过。
- Windows Release 与 NSIS 安装包构建成功。
- 后端 `/api/health` 返回 `{"status":"ok"}`。
- 实际窗口中已确认深灰色标题栏及红色 Logo；启动页仍停留在“正在启动后端进程”，本次未修改工作区已有的启动状态逻辑，未验证进入图库后的操作。

## 产物

- `app/suxing-gallery.exe`：新版程序，与同目录 `python-backend.exe` 配套运行。
- `苏醒图库_0.1.0_x64-setup.exe`：新版安装包。
- `tests.log`、`build.log`：验证日志。

本次只提交外观配置、图标、对应测试和说明，保留工作区原有未提交修改。
