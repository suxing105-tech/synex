苏醒图库 v0.2.1

- 导入窗口新增「选择文件夹」，使用 Windows 系统目录选择窗口；仍可手动输入路径。
- 选择取消时保留原路径，选择完成后点击「开始导入」才保存和扫描。
- 导入前检查目录；新增监听目录会保留已有目录，并按规范化路径去重。
- 新增目录立即接入 Live 监听，无需重启图库。
- 导入错误直接显示在窗口内；空目录及部分内容无法读取时给出明确提示。
- 导入期间防止重复提交，兼容更新前的数据保护。
