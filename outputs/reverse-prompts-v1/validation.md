# 图片反推功能验证报告

日期：2026-09-17。

## 结果

| 验证 | 结果 |
|---|---|
| 后端全量 pytest | 217 passed；1 条依赖弃用警告 |
| 前端全量 Vitest | 36 个文件，316 passed |
| svelte-check | 0 errors；25 条现有组件警告 |
| Vite 生产构建 | 通过 |
| PyInstaller Windows sidecar 构建 | 通过 |
| Tauri Windows release 构建 | 通过，未生成安装器或发布更新 |
| git diff --check | 通过 |

## 运行联调

通过 `smoke.py` 启动已打包的 Windows 后端和本机 Mock 多模态接口，全部使用 `smoke-data/` 隔离目录及虚构密钥。

- 保存模型配置，API 响应仅返回密钥是否已配置。
- 测试接口接收内置测试图；Mock 检查鉴权、路径前缀、实际 Base64 图片。
- 导入合成图片，反推并取得中英双语结果。
- 编辑另存，原始版本保留。
- 重启打包后端，历史仍可读取，密钥可正常解密，再次反推成功。
- 使用生产前端连接此打包后端，实际点击左下角设置、模型表单、测试图片识别、重新反推、中英切换、编辑另存、历史版本；均得到预期界面状态，页面无 error 日志。
- 复制中文、英文、双语的准确文本和取消编辑行为通过组件测试验证。

## 交付文件

- `app/suxing-gallery.exe`：桌面程序。
- `app/python-backend.exe`：配套后端，两者必须保持同目录。
- `artifact-hashes.json`：两个可执行文件的 SHA-256。
- `implementation.md`：使用说明和接口行为。
- `smoke.py`：可复现的打包后端联调脚本。

## 验证边界

未使用真实供应商凭据或调用付费模型；不同供应商的实际识别质量、额度和兼容性需用户在设置中测试。桌面 release 已构建；本次交互检查使用生产前端加打包后端，未启动完整原生窗口连接用户日常图库，也未安装或自动发布更新。

修复了类型检查暴露的既有接口类型缺失（ImageSummary、删除/揭示路径响应、Tauri invoke、Stats），并补充 @types/node。现有项目文件和无关未跟踪文件均保留。
