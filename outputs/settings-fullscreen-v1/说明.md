# 全屏设置与齿轮入口交付

- 左下入口使用 28px 线条齿轮，无「设置」文字；48px 点击区，在底部 96px 区域垂直居中靠左，离窗口底部 24px。
- 设置覆盖整个应用窗口，左侧分类，右侧限宽内容独立滚动；增加标题字号与留白。
- 继续使用底色悬停和品牌色选中，不新增高亮边框。
- 快捷键、模型、通用、ComfyUI、更新功能保持原有逻辑。

验证：332 项 Vitest 通过；svelte-check 0 错误、25 项既有警告；Vite production build 通过。浏览器实测全屏布局、快捷键页、关闭返回、纯图标尺寸及对齐（1280×720）。本次没有修改后端业务或图库数据。

Windows 程序：app/suxing-gallery.exe。与同目录 python-backend.exe 一起保留。关闭旧版后打开新版；未覆盖旧版。原生窗口视觉未自动化验证。
