# 桌面启动超时修复

## 修复

- 清理已确认占用 8765 的旧安装版残留后端。
- 后端监控桌面父进程和 PyInstaller 启动器，防止残留服务进程。
- Rust 在端口冲突和进程退出时立即报告具体错误，超时后清理进程。
- 前端通过状态快照补齐可能错过的 READY 事件，避免重复初始化。
- 后端管道显式使用 UTF-8，避免中文日志导致读取中断。

## 验证

- 前端 288 项测试通过。
- 后端 159 项测试通过。
- Rust 2 项测试通过。
- 最终打包后端通过 READY、健康接口、UTF-8 日志、父进程退出、端口释放和端口冲突验证。
- 已实际观察到修复后的桌面版进入图库主界面，深灰色标题栏和品牌 Logo 正常。
- 用户按 Esc 停止了后续桌面操作，因此没有继续执行关闭和重开界面验证；保留当前运行窗口。

## 产物

- `苏醒图库_0.1.0_x64-setup.exe`：最终安装包。
- `app-final/suxing-gallery.exe` 与同目录 `python-backend.exe`：配套运行文件。
- `verify_packaged_backend.py`：实际打包后端回归验证脚本。
- `frontend-tests.log`、`backend-tests.log`、`rust-tests.log`、`packaged-tests.log`、`build.log`、`bundle.log`：验证及构建日志。
